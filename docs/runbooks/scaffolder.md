# Scaffolder Template (Day 2)

Template: **FastAPI Service (Golden Path)**

- Template definition: `backstage/templates/fastapi-service/template.yaml`
- Rendered skeleton (reference): `templates/output/fastapi-goldenpath-example/`

To enable auto GitHub repo creation, configure Backstage GitHub integration and replace the `debug:log` step with `publish:github`.
