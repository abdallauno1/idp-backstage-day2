apiVersion: v1
kind: Service
metadata:
  name: sample-service-fastapi
  namespace: sample-service
spec:
  selector: { app: sample-service-fastapi }
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
